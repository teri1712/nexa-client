1. i just remove '
   @if (isSendingMessage(msg) && msg.status === 'sending') {
   part in mes list html, and be aware that i just modified mes list compoonent with my idea
2. for message list, please implement this feature: always keep bottom scroll whenever new mesages is appended if
   current scroll is alr bottom, so that user can focus on newest messages